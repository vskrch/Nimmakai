# Scratchpad

## Current task
Multi-provider OpenRouter-style gateway on `development` — implement + commit.

## Branches
- `main` — stable published
- `development` — active (this work)

## Progress
[X] ProviderStore + ProviderHub
[X] refresh_from_hub + namespaced models
[X] Fallback routes per provider
[X] Admin CRUD /admin/providers
[X] Tests (66)
[ ] Commit + push development
