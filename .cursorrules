# Scratchpad

## Current task
All done — merged to main, Heroku-ready, 97 tests.

## Branches
- `main` @ 162ddf3 — stable published (default), Heroku-deployable
- `development` @ 162ddf3 — active (same tip as main)

## Final state (v0.4.0)
- [X] Multi-provider OpenRouter-style gateway (NIM + any OpenAI-compatible)
- [X] Provider CRUD API + web dashboard
- [X] Per-intent user preferences + intelligent ladder fallback
- [X] Context window discovery, exponential backoff, online learning
- [X] 97 tests, ruff clean
- [X] Heroku Procfile + runtime.txt
- [X] Dead code cleanup (circuit.py removed)
- [X] CORS through Settings model
- [X] Comprehensive README with architecture, setup, troubleshooting, Heroku guide

## How to deploy
```bash
heroku create my-nimmakai
heroku config:set PROXY_API_KEYS=sk-key NIM_API_KEYS=nvapi-key
git push heroku main
```
