# Scratchpad

## Current task
Prepare Nimmakai for DigitalOcean App Platform deploy + GitHub CI/CD (~$12/mo) — COMPLETE

## Plan
- [X] Dockerfile (multi-stage: frontend + Python) — smoke tested healthy
- [X] .do/app.yaml (App Platform, auto-deploy from GitHub)
- [X] docs/digitalocean.md + README Deploy section
- [X] GitHub Actions deploy-digitalocean.yml
- [X] docker-compose.do.yml for persistent Droplet path
- [X] scripts/do-smoke.sh

## Budget
- Recommended: apps-s-1vcpu-1gb-fixed (~$10/mo)
- No managed DB; App Platform = ephemeral SQLite (keys via env)

## Lessons
- App Platform does NOT support volumes — document ephemeral SQLite clearly
- Hatch force-include of missing static/dist breaks local installs — copy dist post-pip in Docker instead
- Colima must be started before docker build on this machine
