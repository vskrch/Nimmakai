# Scratchpad

## Current task
Unbiased audit → make router fully functional, resilient, reliable, OSS. DONE (pending user commit).

## Progress
[X] Audit + critical/high reliability fixes
[X] OSS hygiene (LICENSE, packaged YAML, CI, honest README)
[X] 54 tests pass, ruff clean
[ ] Commit when user asks

## Lessons
- Sticky must not hash the entire proxy API key
- Release AccountGuard only after stream iterator finishes
- Empty PROXY_API_KEYS requires ALLOW_INSECURE_AUTH
- Throttle probes with probe_every_n_refreshes
- Empty KeyPool fail-closed > placeholder NIM keys
